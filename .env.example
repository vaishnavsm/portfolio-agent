# Kado exposes an OpenAI-compatible Chat Completions API.
OPENAI_KEY=

# Optional starter overrides. The defaults are shown below.
# OPENAI_BASE_URL=https://awesome.kado.so/openai/v1
# OPENAI_MODEL=kado

# Set this to the production origin used in metadata and generated machine files.
# Vercel preview deployments are detected automatically.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
